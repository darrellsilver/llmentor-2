import { NewPipelineForm } from '@/components/pipelines/pipeline-form';

export default function PipelineDesignerIndexPage() {
  return (
    <section className="flex h-full flex-1 items-center justify-center">
      <NewPipelineForm />
    </section>
  )
}
